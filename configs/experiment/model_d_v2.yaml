# Modelo D v2: PPO + subgrafo H_t + DTQW con λ=0 y log_wealth.

defaults:
  - ../data/nivel1
  - ../env/v2
  - ../agent/ppo
  - ../graph/default
  - ../quantum/default

agent:
  model: D

training:
  total_steps: 50000
  eval_every: 2000
  snapshot_every: 10000
  mlflow_experiment_name: model_d_v2

seeds: [42, 123, 456, 789, 1024]
